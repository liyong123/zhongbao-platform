import withStyles from 'isomorphic-style-loader/lib/withStyles';
import scrollAreaStyle from '../less/scrollArea.less';
import ScrollArea from './ScrollArea';

export default withStyles(scrollAreaStyle)(ScrollArea);
